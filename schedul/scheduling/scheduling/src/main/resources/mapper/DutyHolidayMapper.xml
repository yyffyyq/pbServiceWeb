<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zjintu.schedul.mapper.DutyHolidayMapper">

    <select id="selectDeletedDate" resultType="com.zjintu.schedul.model.entity.DutyDay.DutyHoliday">
        SELECT * FROM duty_holiday
        WHERE holiday_date = #{formattedDate}
          AND is_delete = 1
            LIMIT 1
    </select>

    <update id="updateByHolidayId">
        UPDATE duty_holiday
        SET is_delete = 0
        WHERE id = #{id}
          AND is_delete = 1
    </update>
</mapper>